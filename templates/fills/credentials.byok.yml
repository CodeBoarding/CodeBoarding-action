          # Your own {{LABEL}} key: every run calls {{LABEL}} directly with it.
          # Add {{SECRET}} under Settings → Secrets and variables → Actions. Until it
          # exists the run FAILS and says so. It does not fall back to a different
          # provider on CodeBoarding's hosted tier.
          llm: {{LLM}}
          {{KEY_INPUT}}: ${{ secrets.{{SECRET}} }}
